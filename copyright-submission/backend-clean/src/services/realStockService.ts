import { prisma } from '../config/database';

import { AggregatedStockService } from './aggregatedStockService';
import { logger } from '../utils/logger'에러가 발생했습니다'Batch update failed:'에러가 발생했습니다'1month'에러가 발생했습니다'1week':
          startDate.setDate(endDate.getDate() - 7);
          break;
        case '1month':
          startDate.setMonth(endDate.getMonth() - 1);
          break;
        case '3months':
          startDate.setMonth(endDate.getMonth() - 3);
          break;
        case '6months':
          startDate.setMonth(endDate.getMonth() - 6);
          break;
        case '1year'에러가 발생했습니다'asc'에러가 발생했습니다'T'에러가 발생했습니다'T'에러가 발생했습니다'day' | 'week' | 'month' | '3month' | 'year' = 'day') {
    try {
      
      return { imageUrl: '', error: 'Chart service temporarily disabled'에러가 발생했습니다'035420', '035720', '036570', '251270', '293490'];
      const market = kosdaqStocks.includes(stockCode) ? 'KOSDAQ' : 'KOSPI'에러가 발생했습니다'KOSPI' | 'KOSDAQ' = 'KOSPI'에러가 발생했습니다'desc'에러가 발생했습니다'Scraper service disabled');
  }
}