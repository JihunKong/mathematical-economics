import cors from 'cors'에러가 발생했습니다',') || ['http://localhost:5173'에러가 발생했습니다'Not allowed by CORS'));
    }
  },
  credentials: true,
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],
  allowedHeaders: ['Content-Type', 'Authorization'],
};