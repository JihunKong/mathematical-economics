import { NavLink } from 'react-router-dom';
import { useAppSelector, useAppDispatch } from '@/hooks/useRedux';
import { toggleSidebar } from '@/store/uiSlice';
import clsx from 'clsx';

const menuItems = [
  {
    title: '대시보드',
    path: '/dashboard'에러가 발생했습니다'관심종목',
    path: '/watchlist-setup'에러가 발생했습니다'주식 거래',
    path: '/trading'에러가 발생했습니다'포트폴리오',
    path: '/portfolio'에러가 발생했습니다'리더보드',
    path: '/leaderboard'에러가 발생했습니다'교사 대시보드',
    path: '/teacher'에러가 발생했습니다'주식 가격 관리',
    path: '/stock-management'에러가 발생했습니다'관리자 페이지',
    path: '/admin'에러가 발생했습니다'TEACHER';
  const isAdmin = user?.role === 'ADMIN';
  const isStudent = user?.role === 'STUDENT'에러가 발생했습니다'fixed inset-y-0 left-0 z-50 w-64 bg-white shadow-lg transform transition-transform duration-200 ease-in-out lg:static lg:inset-0',
          {
            'translate-x-0': sidebarOpen,
            '-translate-x-full'에러가 발생했습니다'flex items-center p-2 rounded-lg transition-colors',
                    {
                      'bg-primary-100 text-primary-700': isActive,
                      'text-gray-700 hover:bg-gray-100'에러가 발생했습니다'ko-KR')}
                </p>
              </div>
            </div>
          )}
        </div>
      </aside>
    </>
  );
}